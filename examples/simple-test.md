# Simple Test

Create a file called `hello.txt` in the current directory with the content "Hello, World!".

Then verify the file exists and contains the expected content.

If successful, write the success status. If anything fails, write the failure status with an error message.
